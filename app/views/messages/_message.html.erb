<div id="message-<%= message.id %>">
  <small>
  <!-- <strong class="message-avatar"><%= message.user.avatar %></strong> -->
    <strong><%= message.user.email %></strong>
    <i><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
  </small>
  <p><%= message.content %></p>
</div>
