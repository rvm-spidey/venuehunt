  <%= simple_form_for([@venue, @review],  data: {insert_in_list_target: "form", action: "submit->insert-in-list#send"}) do |f| %>
    <%= f.input :content %>
    <%= f.input :venue_id, as: :hidden, input_html: { value: @venue.id } %>
    <%= f.button :submit %>
  <% end %>
